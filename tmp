# import psycopg2

# Database connection

# conn = psycopg2.connect(
#     host="localhost",
#     database="pinball",
#     user="postgres",
#     password=""
# )
# cursor = conn.cursor()
    
# Insert data into the PostgreSQL database
# cursor.execute("INSERT INTO players (firstname, lastname, nickname, email, phone) VALUES ({fname}, {lname}, {nickname}, {email}, {phone});")
# conn.commit()

# cursor.close()
# conn.close()